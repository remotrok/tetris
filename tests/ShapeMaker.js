var expect = require("expect.js");

suite('ShapeMaker');

test('koko', function() {
	expect(1).to.be.ok();
});

test('koko', function() {
	expect(1).to.be.ok();
});

test('popo', function() {
	expect(1).to.not.be.ok();
});

test('koko', function() {
	expect(1).to.be.ok();
});
